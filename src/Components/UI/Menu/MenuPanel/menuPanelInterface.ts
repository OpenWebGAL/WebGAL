/**
 * @interface
 * Menu页面的按钮的参数接口
 */
export interface IMenuPanel {
    clickFunc?: any,
    buttonOnClassName?: string,
    tagColor?: string,
    iconColor?: string,
    tagName?:string,
    iconName: string,
}